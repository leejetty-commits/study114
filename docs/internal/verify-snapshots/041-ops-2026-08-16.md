# verify 스냅샷 — 운영 study114

- 시각: 2026-08-16 (KST, 에이전트 점검)
- 환경: https://study114.dothome.co.kr
- 배포: Actions Deploy #146 · commit `38958ee` (+ 후속 mock금지 커밋 예정)
- 런북: [44-list-sort-ops-runbook.md](../44-list-sort-ops-runbook.md)

## A. 스키마 (API 간접 확인)

`GET /api/handoff/recommendations.php`

```json
{"ok":true,"status":{"has_table":true,"has_room_column":true,"has_tutor_column":true,"ready":true}}
```

→ 041 `recommend_count` + 042 `user_recommendations` **운영 적용됨**.

phpMyAdmin에서 사용자가 확인한 SHOW:
- `study_rooms.recommend_count` int unsigned NOT NULL default 0
- `tutors.recommend_count` int unsigned NOT NULL default 0
- `user_recommendations` 테이블 존재
- `provider_reviews` 테이블 존재

## B. verify SQL / tutor 진단 원문 (2026-08-16 phpMyAdmin)

### metrics
| metric | n |
|--------|---|
| tutors_total | 8 |
| tutors_published | 2 |
| tutors_expanded_complete | **0** |
| tutors_published_and_expanded | **0** |
| tutors_with_university_name | 5 |
| tutors_sky_name_match | 4 |

### 상태 분포
| profile_status | detail_completion_status | n |
|----------------|--------------------------|---|
| published | basic_only | 2 |
| draft | basic_only | 4 |
| draft | expanded_in_progress | 2 |

### 공개 2건 (검색 후보이지만 API 제외됨)
| id | name | status | completion | university | recommend_count |
|----|------|--------|------------|------------|-----------------|
| 1 | 대치 Prime 수학 | published | **basic_only** | 서울대 | 0 |
| 2 | 우동 Pick 영어 | published | **basic_only** | 연세대 | 0 |

### recommend / reviews
- recommend_count: 전원 0 (min=max=0)
- provider_reviews tutor visible: 0
- provider_reviews room visible: (쿼리 실행됨 · 값은 사용자 화면 후속)

## C. tutor 0건 — **확정 결론**

| 질문 | 답 |
|------|----|
| 정렬 버그? | **아니오** (API 200, sort 정상) |
| 운영 데이터/노출조건? | **예** |
| 직접 원인 | 검색 WHERE가 `published` **AND** `expanded_complete` 인데, 운영에 `expanded_complete` tutor가 **0건** |
| SKY 데이터 자체 | 있음 (서울대/연세대 등 4건 name match) — 다만 **전부 비노출 상태**(basic_only 또는 draft) |
| recommend/sky 화면 검증 | **현재 불가** — 노출 가능 row 없음 |

제품 규칙(상세완료 후 목록)을 유지한다.  
**금지:** id 1·2를 수동 `UPDATE ... expanded_complete` 로 올리는 임시 검증.

후속 구조 수정: [45-tutor-expanded-complete-ssot.md](../45-tutor-expanded-complete-ssot.md)  
- 필드 기반 `TutorDetailCompletionEvaluator`
- Admin `recompute-tutor-detail` (필드 충족 row만 승격)
- 부족 항목은 diagnose SQL `miss_*` / API `detail_missing`

## D. 추천 API 운영 로그인 검증 (2026-08-16)

계정: `guardian1@dev.local` (세션 user id 6)  
대상: study_room id=1

| 항목 | 결과 |
|------|------|
| 추가 | PASS recommended=true count=1 |
| 취소 | PASS recommended=false count=0 |
| 재추천 | PASS count=1 |
| 연타 토글 | PASS (0↔1 교차) |
| bad type student | PASS HTTP 422 |
| bad id | PASS HTTP 422 |
| 비로그인 POST | PASS 401 (이전 점검) |

로컬 Docker `scripts/smoke-recommend-integrity.php`: **PASS**

## E. API 회귀 (동시각)

| 케이스 | 결과 |
|--------|------|
| room latest/recommend/review | 200 |
| room sky → latest | PASS |
| student sky → latest | PASS |
| tutor * | 200 · total=0 |

## G. 구조 수정 후 로컬 검증 (2026-08-16, Docker)

- `TutorDetailCompletionEvaluator` + save 경로로 tutor id=1 상세필수 충족
- **status 컬럼 수동 UPDATE 없이** `expanded_complete` 자동 반영
- `published` + 이미지 후 `published_and_expanded=1`
- SearchService tutor `latest`/`recommend`/`sky` **total=1**

운영은 배포 후 Admin `recompute-tutor-detail` + diagnose `miss_*` + 소유자 상세보강.  
문서: [45-tutor-expanded-complete-ssot.md](../45-tutor-expanded-complete-ssot.md)
