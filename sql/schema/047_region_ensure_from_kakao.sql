-- =============================================================================
-- study114 schema 047 — 카카오 주소검색으로 행정동 적재
-- DDL 없음. 런타임 RegionEnsure::fromKakao 가 regions 행을 멱등 INSERT 한다.
--
-- 전국 수천 개 행정동을 미리 시드하지 않는다.
-- 이용자가 우편번호에서 고른 동의 법정동코드(bcode)·동 이름으로 마스터를 채운다.
-- 아파트단지(complexes)도 같은 방식(ComplexEnsure).
-- =============================================================================

SELECT 1;
