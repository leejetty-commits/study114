-- =============================================================================
-- study114 schema 052 — [폐기] 쪽지 기본값 paused
-- 정본 교체: 067_publish_inquiry_split_address_zip.sql (DEFAULT open)
-- 이 파일은 역사 기록용. 신규 DB에 적용하지 말 것.
-- =============================================================================
