/**
 * Plans screens extension — checkout / result / history extras.
 *
 * GROWTH LANDING: 결제·결과·이력 부가 UI·헬퍼는 여기에 추가한다.
 * `screens.js`는 라우팅·이벤트 허브로 유지.
 *
 * 기존 renderPlansCheckout·Result·History를 한꺼번에 옮기지 말 것 (고위험).
 */

/** @type {const} */
export const SCREENS_EXT_CHECKOUT = 'checkout';
