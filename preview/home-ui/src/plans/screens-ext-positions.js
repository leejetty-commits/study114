/**
 * Plans screens extension — positions / storefront.
 *
 * GROWTH LANDING: 노출상품(positions) 신규 UI·헬퍼는 여기에 추가한다.
 * `screens.js`는 renderPlansScreen / bindPlansScreenEvents 허브로 유지.
 *
 * 기존 renderPlansPositions·bind 로직을 한꺼번에 옮기지 말 것 (고위험).
 */

/** @type {const} */
export const SCREENS_EXT_POSITIONS = 'positions';
