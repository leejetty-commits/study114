/**
 * Plans screens extension — access / tickets.
 *
 * GROWTH LANDING: 쪽지권(access)·티켓 관련 신규 UI·헬퍼는 여기에 추가한다.
 * `screens.js`는 라우팅·이벤트 허브로 유지.
 *
 * 기존 renderPlansAccess·bind 로직을 한꺼번에 옮기지 말 것 (고위험).
 */

/** @type {const} */
export const SCREENS_EXT_ACCESS = 'access';
